const initialState = {
  name: "",
  email: "",
  password: "",
  confirmPassword: "",
};

export const formReducer = (state = initialState, { type, action }) => {
  switch (type) {
    default:
      return state;
  }
};
